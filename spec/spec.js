var myCode = require('../src');

describe('spiralMatrix', function() {
  it('returns the input integer', function() {
    expect(myCode.spiralMatrix(14)).toEqual(14);
  });
});

// describe('matches', function() {
//   it('returns true if the given arguments match', function() {
//     expect(yourCode.matches('a', 'a')).toEqual(true);
//   });
// });
